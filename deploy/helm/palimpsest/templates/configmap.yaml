apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "palimpsest.fullname" . }}-config
  labels:
    {{- include "palimpsest.labels" . | nindent 4 }}
data:
  palimpsest.toml: |-
    {{- .Values.config | nindent 4 }}
